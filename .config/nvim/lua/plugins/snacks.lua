return {
  "folke/snacks.nvim",
  opts = {
    dashboard = {}, -- other snacks config
    -- Add file explorer config here if snacks manages it
    explorer = {
      show_hidden = true, -- This might be the setting name
    },
  },
}
